#include <stdio.h>{
    int num = 1;
    do{
        if (num % 7 == 0) {
            printf("первое числоб кратное 7: %d\n", num);
            break;
        }
        num++
    } while (num <= 20);
    return 0;
}